Y = new Array();

for(var i=0; i<initialMatrix.length; i++) {
	Y[i] = new Array();
	var iM = initialMatrix[i][2];
	if(('R' in iM) AND ('X' in iM)) {
		Y[i]
	} else {
		console.log('R not exists...');
	}
}